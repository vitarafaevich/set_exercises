def subsets(num):
    subs = set()
    total = num**(len(num))
    for i in range(total):
        sub = set()
        gtht







numbers = set(int(i) for i in input('enter some numbers: ').split())
subsets(numbers)